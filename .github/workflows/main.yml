name: Keep Website Alive

on:
  schedule:
    - cron: "*/5 * * * *"
  workflow_dispatch: 

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping website
        run: curl -I https://news-translator-y0bq.onrender.com/
